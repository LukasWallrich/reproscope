You are extracting every reported statistical result from a social-science paper. The paper's pages are attached as images, in order. Use the images as the source of meaning; native text and coordinates assist navigation and transcription.

Return a JSON object with `claims` and `regions` lists. One entry per reported quantity: every coefficient, effect size, test statistic, p-value, confidence-interval bound, mean, standard deviation, sample size (including recruited, excluded and demographic counts, also when spelled out) and percentage that the paper reports as a result of its own analysis, in tables, figures (where a value is printed), and in the text. Do not include numbers that describe the design (years, scale ranges, number of items, thresholds, stimulus durations) or numbers cited from other papers.

Each entry:
- `claim_id`: "c" + running number in reading order, e.g. "c001".
- `study_id`: "study1", "study2", ... ("study1" if the paper has one study).
- `claim_type`: "scalar" (a single number in text), "table_cell", "figure", "range".
- `importance`: "headline" if the quantity is one the abstract or the main hypothesis test rests on, else "supporting".
- `quantity_kind`: one of coefficient, se, p_value, t, F, chi2, z, d, r, eta2, OR, mean, sd, n, ci_lower, ci_upper, percent, other.
- `value`: the number as printed (numeric; for "p < .001" give 0.001 and set `comparator` to "<"). `comparator`: "=" / "<" / ">" / "<=" / ">=".
- `precision`: number of decimals as printed.
- `location`: {"page": integer page number in the PDF (1-based, counting the images), "kind": "table" | "figure" | "text", "label": e.g. "Table 2", "cell": row and column headers for table cells, e.g. "Model 2 / Openness (b)"}.
- `description`: a short description of what this quantity is, as the paper words it (which outcome, which predictor, which sample, which test). Copy the surrounding sentence for text results.
- `analysis_label`: a short label for the analysis the quantity belongs to, so that all quantities from one model share a label (e.g. "Study 1 regression of X on Y with covariates"); use the same label for all cells of one model.

Be exhaustive on tables: extract every cell that holds a result. Be exact about values; do not round. If a page image is unreadable, note it in `notes` and continue. Output only JSON.

For every quantity return source_quote (a short exact printed clause containing the statistic and operator, without ellipses), source_region (stable sentence/table-cell/figure-panel and bracket description), quantity_role (inferential, descriptive, supplied_fact, or unknown), aggregation (scalar, all, any, min, max, vector), target_outcome, target_contrast and target_model. Use consistent canonical semantic names across studies. Preserve the printed value even when it looks implausible. Distinguish CI endpoints. Figure annotations require the actual bracket endpoints and legend, not a guessed adjacent comparison. Inventory every text, table and figure region; explicitly note regions with no targets or unreadable targets in notes. Source quote must be copied, not paraphrased.

Return regions alongside claims. Include each page's text and every table/figure region, even if no targets are found. Each region has page (relative to supplied images), kind (text/table/figure), region_id, status (complete/no_targets/unreadable), and exact claim_ids. Every supplied page needs at least one region. An unreadable region is an explicit coverage gap, not no_targets.

For figure significance annotations also return figure_panel, the two distinct figure_endpoints (the actual bracket endpoints), and exact legend_quote. A legend threshold without identifiable bracket endpoints cannot establish a comparison.

For text, make source_quote long enough to identify one occurrence on its page (e.g. include the associated statistic and context rather than a repeatedly printed p threshold). Use natural PDF reading order across columns. Classify analysed sample size as quantity_role="descriptive". Every empirical number, including sample counts and descriptives, must be checked; quantity_role does not control whether computation is required. supplied_fact identifies stated design/administrative information whose implementation or unavailable evidence must also be accounted for.

Physical source identity: a native-PDF list supplies source_token_id identifiers, coordinates and local text. Select the identifier for the exact numeric occurrence you read, independently of your interpretation of its value or meaning. For a figure significance bracket, select its star/annotation marker identifier, NEVER the shared legend threshold identifier; preserve the actual panel and bracket endpoints separately. The candidate list includes design settings, axes, references and equation constants: select only in-scope results. Inspect the full images for missing/image-only results and use source_token_id=null when no physical candidate represents a result. Do not invent IDs or force a reading onto a nearby occurrence. Source quote is explanatory evidence; physical identity need not depend on reconstructing a sentence across PDF columns, figures or broken subscripts.

Separate the measurement model from the reported statistical analysis in your interpretation: target_model identifies the statistical test or estimator for THIS reported quantity (e.g. paired_t, pearson_correlation, likelihood_ratio, descriptive_summary), not an upstream model that generated its input measurements. Use source evidence and unknown where appropriate. For a model parameter such as p_g, return its actual quantity type (mean/coefficient/other); the letter p in its name does not make it a p-value. Recruited, excluded and analysed counts are descriptive; analysed N must retain its study/sample association. Degrees of freedom accompany the test statistic's evidence and must be checked against the computed test; do not add them as independent effect estimates.
