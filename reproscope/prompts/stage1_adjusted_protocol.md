Additional closed verification forms apply to directly bound adjusted analyses in this packet. Keep protocol_version="direct-plan-3". Use these supported forms when appropriate; never substitute an unadjusted test for an adjusted analysis.

For partial Pearson correlations: {analysis_id,status:"supported",family:"partial_correlation",file,table,header:0,x:<one variable>,y:<other variable>,covariates:[<every exact adjustment column>],predictors:[],coefficient:null,coefficient_field:null,id_column,included_ids,missingness:"complete_cases" or "complete_explicit_sample",numeric_parsing:"strict_float" or "strict_float_blank_missing",alternative:"two-sided",transformations:[],intercept:true,covariance:"classical",confidence_level:0.95}. Residualise both variables on an intercept and all covariates; inference uses n-k-2 df for k covariates. Analyse complete cases jointly across all variables.

For classical OLS: same fields, family:"linear_regression", x:<outcome>,y:null,covariates:[],predictors:[<all exact predictor and adjustment columns, no duplicates>],coefficient:<target predictor column, or null for overall model>,coefficient_field:<its exact intake contract_field, or null for overall model>. Include an intercept. Coefficients, standardised beta, coefficient t/SE/CI and overall model F/R² are distinct quantities: use the requested quantity identity, never substitute one for another. Treat categorical recodings, nonlinear terms, weighted/robust or transformed fits as unsupported by this direct checker; faithfully compute them if the source and bindings establish them, explaining the verification limit.

Sample traces must derive included_ids from the actual analysed rows after the documented eligibility and missing-data rules. Emit the realised sample even when the eligible ID set was fixed by intake. Each script execution must regenerate its plan and every requested result from the supplied data.
