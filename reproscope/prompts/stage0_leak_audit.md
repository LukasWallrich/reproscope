You are auditing whether a redacted methods document and its contracts leak the study's results. You have not seen the paper. Inspect every analysis for a stated or implied finding, including nested evidence quotations. Report only inferences supported by the supplied text; do not produce a prediction for every analysis from background knowledge. Then rate the leakage.

Keep operational method parameters: eligibility and scoring thresholds, item counts, scale ranges, stimulus timing, cue intensities, payment rates and prespecified analysis windows. A fixed confidence level such as 0.95 in the generic task is a method parameter. Do not report these as leaks merely because they are numeric. Observed recruited, excluded or analysed sample sizes, group or sex counts, observed percentages and participant-age means or SDs ARE empirical results and must be withheld, alongside outcome means/SDs, effect sizes, test statistics and significance statements. The replica must derive realised sample counts from data and declared selection rules. Contrast definitions (A minus B) specify an estimand, not the observed direction. Phrases such as "tests revealed", "showed that", "neither model improved" can disclose results even with the numeric values removed.

Material:
{{blind_material}}

Return concise JSON: {"audited_analysis_ids": ["a01", "..."], "inferred_results": [{"analysis": "...", "inference": "...", "basis": "exact supporting quote", "confidence": "high"|"medium"|"low"}], "leak_rating": "none"|"weak"|"strong", "leaking_passages": [{"quote": "exact passage from supplied material", "kind": "numeric_result|directional_result|structural_cue|general_knowledge"}]}. List all inspected analysis IDs but include inferred_results only for an actual text-supported inference; return an empty list when there are none. "weak" means direction could be guessed only from general knowledge or hypothesis framing; "strong" means the text states or implies a finding. Output only JSON.

The supplied packet is exactly the prose and contract delivered to the replica after readiness filtering. Only state that a result leaks when the packet actually states or implies that finding. Distinguish result statements from choices of model and available statistic fields. General background expectations and structural cues do not establish an explicit result. Quote the exact supporting passage for every finding.

Executable sample selections are inputs: deposited row IDs and column filters established at intake may identify the authorised rows. Their length is observable from the supplied data and is not a published target N. Do not infer recruitment totals or exclusion counts from the largest row ID or gaps between IDs. Distinguish these operational records from prose or design fields reporting the paper's observed sample counts.
