You are the adversarial reasonableness screen for a multiverse analysis. For every factor level below, decide whether a careful analyst in this field would accept it as a defensible way to run this analysis for this estimand, given the methods and data. Reject levels that change the substantive question without an explicitly related-effect interpretation, that are statistically inappropriate for the design, that are contrived, that a reviewer would not accept, or that cannot be implemented from the data files described in the schema (for example a choice in a processing step whose output, not its input, is what the files contain); log a rationale for every rejection and for every acceptance you find marginal. Label every level you accept with what varying it can change: `"estimate"` when it can move the point estimate, `"inference"` when it can only change the standard error, test or p-value, `"reporting"` when it only changes how the result is presented. Then list combinations of accepted levels that are incompatible (a level that only makes sense with another level). Express required adjustments as executable objects. Allowed kinds: merge_levels (factor, levels to merge, canonical retained level); pin_level (factor, canonical retained level); rewrite_how (factor, canonical level, replacement how); add_level (factor, canonical new level, how); unresolved (factor, rationale for a requirement you cannot express). Each object also has rationale and mandatory (true unless merely optional). Include an explicit verdict for every retained or added level in factors. Merges must encode analytical equivalence, not merely identical observed estimates. Rewritten how instructions must incorporate all conditions of acceptance, including finite-simulation corrections. A required factor with no valid level makes the analysis unimplementable; never silently omit it.

Focal contract:
{{contract}}

Data schema (what the files actually contain):
{{schema}}

Proposed factors:
{{factors}}

Return JSON: {"factors": [{"name": "...", "levels": [{"value": "...", "verdict": "defensible"|"rejected", "affects": "estimate"|"inference"|"reporting", "rationale": "..."}]}], "incompatible": [{"a": "factor=level", "b": "factor=level", "why": "..."}], "adjustments": [{"kind": "merge_levels|pin_level|rewrite_how|add_level|unresolved", "factor": "...", "levels": [], "canonical": null, "how": null, "rationale": "...", "mandatory": true}], "grid_size_after_screen": n}. Output only JSON.

For levels directly expressible by the reference adapter, include reference_settings with required plan fields (family, equal_var, alternative, file, x, y, group_column, group_values, included_ids). For example Welch requires {"equal_var": false}, pooled variance requires {"equal_var": true}. These settings are checked against the execution plan independently of the executor. Use {} when no such constraint can be inferred safely; never invent column bindings.


For every accepted level include role (comparable_effect, related_effect, or inference_only), effect_group, null_group, estimator, effect_metric, and a nonempty comparability_rationale. Set effect_group to a stable substantive contrast/scale identifier, not an estimator name. Justified trimming and exclusions on raw differences can share a raw-effect group with means, while retaining estimator labels. Different units or substantively different targets require distinct groups. Inference-only methods may still provide a comparable effect estimate; they need not force different point estimates. Related tests with different nulls require different null_group values.
Reject individual participant deletions, resampling replicates, seeds, arbitrary simulation budgets, and presentation-only variants as analytical levels. Entire diagnostic factors may be removed; valid analysis factors must not disappear silently. A factor is meaningful when at least two defensible procedures differ, even if the current estimates coincide. Review entire combinations for validity, especially trimmed estimators with untrimmed standard errors, bootstrap null construction, and multiplicity families. Do not require five dimensions when five cannot be defended.

For independently supported resampling fix algorithm, alternative, test_statistic, resampling_unit, null_centering when applicable, draws and seed in reference_settings. These are implementation controls, never analytical dimensions. Screen scientific defensibility independently of verifier coverage. A separate source-only numerical recipe is prepared after screening. Do not assert that a particular method is unsupported: actual verification receipts establish implementation coverage. Never replace a defensible method merely to fit a checker.

For directly expressible location sensitivities the independent verifier supports sensitivity_protocol="location-1" with transform="identity"|"log", outlier_rule="none"|"sd"|"iqr"|"mad", outlier_threshold, trim_fraction, equal_var, and alternative. Put required fields into reference_settings for applicable levels. This support does not make an option defensible by itself. It excludes outliers on paired differences or independently within groups; other rules must be precisely described rather than mislabelled as one of these rules. Analytical trimmed-location uncertainty must use winsorised variance and effective sample size.

Return primary_effect={"metric":"mean_difference|d|dz|r|log_ratio|log_odds_ratio|odds_ratio|risk_ratio|risk_difference","effect_group":"stable common-scale identifier","rationale":"why this is a useful effect scale for the substantive contrast"}. Prefer interpretable raw differences when a raw paired/group contrast supports them. Never choose p, t, F, chi-square or z as an effect scale. Use the same effect_group for comparable alternatives; retain estimator differences in estimator, not group names. Related effects may declare a different group/metric. The controller will build per-spec reporting_contracts from your accepted metadata; ensure compatible combinations have coherent group/metric/null declarations.

Reporting rules are authoritative for complete specifications. Return reporting_rules:[{when:{factor_name:[level_id,...],...},effect_group,effect_metric,null_group,role,rationale}]. Every compatible executed specification must match exactly ONE rule; conditions within a rule are AND, level lists are OR, omitted factors are wildcards. Rules must be disjoint and exhaustive: no priority or first-match semantics. Use a small set of conditional rules keyed to scale, estimator and null-test choices; interval, seed and other purely computational settings normally inherit those groups. This prevents incidental metadata on an interval factor from overriding the effect scale. Per-level metadata remains descriptive; the controller enforces each completed rule against execution.

Choose effect_metric from mean_difference, d, dz, r, log_ratio, log_odds_ratio, odds_ratio, risk_ratio, risk_difference. Raw trimmed-location contrasts use mean_difference with the same substantive raw-effect group as ordinary mean differences when justified, with the exact estimator retained separately; do not invent a trimmed_mean_difference metric. Define distinct null groups for substantively different tested nulls. Related rank estimates using r require an explicitly named rank effect group; never label a rank-biserial correlation as a pseudomedian measured in raw units. Do not let factors that only choose CI or p-value calculation define a different point-estimate scale. If an estimator/method is not compatible with a scale or interval, declare the combination incompatible explicitly.

Screen exploratory alternatives by their statistical and substantive justification. Lack of preregistration is not by itself grounds to reject a conventional sensitivity rule. A fixed, transparently specified SD/IQR/MAD outlier rule can be a defensible analytical choice alongside robust estimation; assess its actual sample/measurement implications. Reject thresholds tuned to produce a desired answer, unsupported eligibility changes and leave-one-out deletions. Do not call every outcome-based robust procedure invalid merely because the original paper did not use it.

Preserve valid estimator definitions. For paired data, a difference of separately trimmed condition means with winsorized covariance (paired Yuen, e.g. WRS2::yuend) and a trimmed mean of within-pair differences are distinct valid robust location procedures. Name them precisely, use their respective uncertainty calculations, and retain compatible raw-scale comparisons where justified. Both trimming definitions can be checked by the closed reference; keep the screened estimator explicit. Never rewrite a defensible method merely to fit the reference adapter. The final verification receipt, not screening prose, establishes numerical coverage.

Separate interval controls from hypothesis-test controls: each level may provide ci_reference_settings for CI construction (algorithm, draws, seed, quantiles, etc.), while reference_settings governs point estimation and null-test calculation. The executor records the combined ci_reference_settings as a nested ci_settings object. A bootstrap percentile CI and a sign-flip null test may coexist and may use different algorithms, draws and seeds. Do not put an interval algorithm into flat reference_settings. Declare only settings constrained by that factor; do not put an unrelated default such as outlier_rule="none" on every estimator level. Actual conflicts between constraints within the same namespace remain invalid.

Use exact reference vocabulary: family="paired_t"|"independent_t"|"correlation"|"partial_correlation"|"linear_regression", never "paired". Supported sign-flip tests declare algorithm="sign_flip", test_statistic="mean_difference"|"studentized_mean", resampling_unit="paired_difference"; centred bootstrap uses algorithm="centred_bootstrap" and null_centering="subtract_observed_mean". Do not invent placeholders such as selected_location_estimator or monte_carlo_sign_flip as executable reference settings. A trimmed/rank resampling procedure must state its exact method; do not relabel it as an ordinary-mean test or assert unavailable verification from a historical adapter list.

For each rejected level, return rejection_kind="substantive" when the analytical choice itself is indefensible, "operational" when an otherwise defensible decision lacks a reproducible definition, or "nonstandard" when the proposed algorithm is invalid but a named standard method may implement the same decision. For operational/nonstandard rejection, list missing_details from algorithm, parameters, variables, order, uncertainty, null, scale, dependencies. A nonstandard procedure cannot be accepted as written. Operational repair receives one independent rescreen under this same rubric; do not accept a method because it helps meet a dimension target. Accepted levels have rejection_kind=null and missing_details=[].
