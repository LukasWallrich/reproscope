Below is the result of a multiverse analysis: one row per defensible specification, with the focal estimate. Also given: the factors that were varied, and the paper's reported estimate. Describe the distribution neutrally: the range, the median, the share of specifications with the same sign as the median, the share significant (against each specification's own `p_threshold` where the rows carry one, .05 otherwise) and which factors flip a specification from significant to not, which factors move the estimate most (compare estimates by level holding the other factors fixed where the grid allows, that is a paired comparison across otherwise identical specifications), and where the reported estimate sits relative to the distribution you describe. Work everything out from the specifications below; no summary statistics are supplied. Do not say whether the paper's claim is "supported" or "robust"; report what the curve shows. At most 250 words of Markdown, then a JSON block: {"range": [min, max], "median": ..., "share_same_sign": ..., "share_significant": ..., "top_factors": ["..."], "n_specs": ...}.

Specifications:
{{specs}}

Reported estimate: {{reported}}
Factors: {{factors}}
Coverage: {{coverage}}
